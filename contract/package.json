{
  "name": "night-desk-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "compact": "compact compile invoice.compact managed/night-desk",
    "build": "tsc --project tsconfig.build.json && mkdir -p ../frontend-landing/public/keys ../frontend-landing/public/zkir && if [ -d ./managed/night-desk/keys ]; then cp -Rf ./managed/night-desk/keys/* ../frontend-landing/public/keys/; fi && if [ -d ./managed/night-desk/zkir ]; then cp -Rf ./managed/night-desk/zkir/* ../frontend-landing/public/zkir/; fi",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}