{
  "buildCommand": "cd contract && npm run build && cd ../api && npm run build && cd ../ui && npm run build",
  "outputDirectory": "ui/dist",
  "installCommand": "npm install",
  "framework": null
}
